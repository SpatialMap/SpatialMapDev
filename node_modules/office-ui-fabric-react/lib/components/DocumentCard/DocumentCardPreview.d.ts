import { IDocumentCardPreviewProps } from './DocumentCard.Props';
import { BaseComponent } from '../../Utilities';
export declare class DocumentCardPreview extends BaseComponent<IDocumentCardPreviewProps, any> {
    render(): JSX.Element;
    private _renderPreviewImage(previewImage);
    private _renderPreviewList(previewImages);
}

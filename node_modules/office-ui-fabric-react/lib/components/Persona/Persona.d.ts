import { BaseComponent } from '../../Utilities';
import { IPersonaProps } from './Persona.Props';
export interface IPersonaState {
    isImageLoaded?: boolean;
}
export declare class Persona extends BaseComponent<IPersonaProps, IPersonaState> {
    static defaultProps: IPersonaProps;
    constructor(props: IPersonaProps);
    render(): JSX.Element;
    private _renderElement(text, className, render?);
    private _onRenderInitials(props);
    private _getColorFromName(displayName);
    private _onPhotoLoadingStateChange(loadState);
}

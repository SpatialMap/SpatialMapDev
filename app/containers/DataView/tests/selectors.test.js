// import { fromJS } from 'immutable';
// import { makeSelectDataViewDomain } from '../selectors';

// const selector = makeSelectDataViewDomain();

describe('makeSelectDataViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { fromJS } from 'immutable';
// import { makeSelectDatasetsDomain } from '../selectors';

// const selector = makeSelectDatasetsDomain();

describe('makeSelectDatasetsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

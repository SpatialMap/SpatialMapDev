/*
 * Datasets Messages
 *
 * This contains all the text for the Datasets component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.Datasets.header',
    defaultMessage: 'This is Datasets container !',
  },
});
